#This file is used to package the LCSCAutoImport plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.20.0
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-lcsc-auto-import"
description = "InvenTree plugin for importing and scanning LCSC parts automatically"
dynamic = ["version"]
authors = [
    { name = "Isosloth",email = "ju@isosloth.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/isosloth/inventree-plugin-lcsc-autoimport"

[project.entry-points."inventree_plugins"]
LCSCAutoImport = "lcsc_auto_import.core:LCSCAutoImport"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "lcsc_auto_import.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["lcsc_auto_import"]
